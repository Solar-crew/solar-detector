# Database Configuration
# For local development, these defaults work with docker-compose
# For production, use strong passwords and secure credentials
POSTGRES_USER=user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=solar_detector
POSTGRES_HOST=db
POSTGRES_PORT=5432

# API Configuration
API_V1_PREFIX=/api/v1

# Frontend Configuration
# Update this to your production API URL when deploying
VITE_API_URL=http://localhost:8000
